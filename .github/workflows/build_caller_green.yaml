name: "GREEN: Test Build Caller Pol 1"

# export tt=build-g; git tag -d "$tt"; git push --delete origin "$tt"; git tag "$tt" && git push origin "$tt"

on:
  workflow_dispatch:
  push:
    branches:
      - "test"
    tags:
      - "all"
      - "build"
      - "build-g"

jobs:
  call_test_build_job:
    uses: boromir674/automated-workflows/.github/workflows/test_build.yml@test
    with:
      run_policy: '1'
      typecheck_policy: '1'

      # OS, Python, Build "mode"
      build_installation: 'edit sdist wheel'
      job_matrix: "{\"platform\": [\"ubuntu-latest\"], \"python-version\": [\"3.11\"]}"
